<%= turbo_frame_tag dom_id(response), class: "px-2 rounded-lg inline-block #{response.ai? ? 'self-start bg-blue-900' : 'bg-black self-end'}" do %>  
  <p class=" text-white px-2 py-2 rounded"> <%= response.content  %> </p>
<% end %>